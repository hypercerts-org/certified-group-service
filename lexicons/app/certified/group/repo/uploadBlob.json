{
  "lexicon": 1,
  "id": "app.certified.group.repo.uploadBlob",
  "defs": {
    "main": {
      "type": "procedure",
      "description": "Upload a blob to the group's repository. Proxied through the user's PDS.",
      "parameters": {
        "type": "params",
        "properties": {
          "repo": {
            "type": "string",
            "format": "at-identifier",
            "description": "The handle or DID of the target group. Passed on the querystring because the request body is the raw blob bytes. Optional during the #27 deprecation window: omit it and the group is taken from the (deprecated) JWT aud claim instead."
          }
        }
      },
      "input": {
        "encoding": "*/*"
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["blob"],
          "properties": {
            "blob": { "type": "blob" }
          }
        }
      }
    }
  }
}
