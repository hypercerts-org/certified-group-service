# Server
# Port the HTTP server listens on
PORT=3000

# Public URL of this service (written into group DID documents for atproto-proxy resolution)
SERVICE_URL=https://group-service.example.com

# Storage
# Directory where SQLite databases and group data are stored
DATA_DIR=/app/data

# Security
# 32-byte hex key used for AES-256-GCM encryption of stored credentials
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=

# Group PDS
# URL of the PDS where group accounts are created
GROUP_PDS_URL=https://pds.example.com

# Admin
# OPTIONAL. Password for the operator-only admin XRPC endpoints
# (app.certified.group.admin.*), authenticated via HTTP Basic auth with
# username "admin", mirroring com.atproto.admin.* on a PDS. Leave unset to
# disable all admin endpoints entirely. Must be at least 16 non-whitespace
# characters when set. (Mirrors the reference PDS's PDS_ADMIN_PASSWORD.)
# CGS_ADMIN_PASSWORD=

# atproto
# PLC directory URL
PLC_URL=https://plc.directory

# DID document cache TTL in milliseconds (default: 10 minutes)
DID_CACHE_TTL_MS=600000

# Blobs
# Maximum blob upload size in bytes (default: 5 MB)
MAX_BLOB_SIZE=5242880

# Logging
# Log level: trace, debug, info, warn, error, fatal
LOG_LEVEL=info

# Version
# OPTIONAL operator override for the version string reported by /health and
# /xrpc/_health. Leave unset: the version is normally stamped at build time
# (.cgs-version) or read from package.json. Only set this to force a value.
# CGS_VERSION=0.1.0+abcdef01
